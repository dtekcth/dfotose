version: '3.7'
services:
  dfotose:
    build: .
    environment:
      - NODE_ENV=production
    ports:
      - 127.0.0.1:4000:4000
    volumes:
      - storage:/dfotose/storage
      - images:/dfotose/uploaded-images
    depends_on:
      - mongo
      - redis
  redis:
    image: "redis:alpine"
    restart: unless-stopped
  mongo:
    restart: unless-stopped
    image: "mongo:4.0.1"
    environment:
      - MONGO_DATA_DIR=/data/db
    volumes:
      - dfoto-mongo-data:/data/db
    command: mongod --smallfiles

volumes:
  dfoto-mongo-data:
  storage:
  images:
