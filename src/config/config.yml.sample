default:
  port: 4000

  session:
    static: true
    secret: 'foobarwithdragons'

  database:
    host: 'localhost'
    name: 'dfotose'

  redis:
    host: 'localhost'

  storage:
    path: './storage/images/'
    temporaryImagePath: './uploaded-images/'
